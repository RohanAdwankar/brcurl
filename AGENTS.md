# AGENTS.md

Always use UV when working with Python.

Reflection Notes

- Keep test and fixture workflows outside the main CLI unless the user explicitly asks to expand the product surface area.
- When a request is ambiguous about implementation shape, ask before changing command structure or adding subcommands.
